# shellcheck shell=bash
# Network helpers, package manager policy, and Certbot environment bootstrap.
# All canonical implementations now live in core.sh (network/pkg) and certops.sh (certbot).
# This file exists for backward compatibility with the module loading system.
:
