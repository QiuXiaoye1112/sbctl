# shellcheck shell=bash
# Transactional config/metadata state management.
# All canonical implementations now live in engine.sh and inbound.sh.
# This file exists for backward compatibility with the module loading system.
:
